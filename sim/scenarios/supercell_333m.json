{
  "schema_version": "1.0",
  "name": "supercell_333m",
  "kind": "supercell",
  "phase": "phase3-T1",
  "description": "The Phase 3 supercell: a classic Weisman-Klemp splitting supercell at 333 m, in a Bunkers-tracked moving domain. This is the first SHEARED, MOVING, ASYMMETRIC scenario in the project. Physically it is the Phase 0 validation storm (canonical WK supercell -- deep sustained updraft, split into counter-rotating left/right movers, peak w 60.6 m/s @ 83 min) promoted to the production microphysics (NSSL ptype=27) and the 333 m overnight tier. As the deck-generator advisor guessed and the plan (docs/phase3-plan-2026-07-20.md 2.2) confirmed, the base template IS that validation deck, so this scenario mostly STOPS overriding it: single_cell drove iwnd->0 and imove->0 to get a zero-shear pulse cell; the supercell keeps the template's iwnd=2 shear and imove=1 moving frame. Its asymmetric, splitting storm is what discharges the Phase 2 T9 cref orientation test (plan 2.4).",

  "sim": {
    "run_dir": "/home/boiko/thunderstorm/runs/supercell333",

    "namelist": {
      "_note": "Deltas FROM single_cell_333m (which is the zero-shear baseline): iwnd 0->2 (WK unidirectional shear), imove 0->1 (Bunkers moving frame) with umove/vmove now REQUIRED and non-zero, and the domain grows to 360x360 to hold the split. ptype=27 and the vertical grid are unchanged from the baseline. Every REQUIRED_KEY is declared explicitly (deck.py does NOT default them) even where the value matches the template -- that is the design: generating the scenario must exercise every override, never silently inherit.",

      "nx": 540,
      "ny": 540,
      "nz": 40,
      "dx": 333.0,
      "dy": 333.0,
      "dz": 500.0,

      "_domain_note": "540 x 333 m = 179.82 km (half-width 89.91 km). ENLARGED from the Phase 0 120 km grid after the FIRST T1 run (360x360, 2026-07-21) empirically resolved watch-item 1: the Phase 0 flat/imove containment (120 km held the split with 26 km clearance) was measured at 1 km/Morrison, and it does NOT hold at 333 m/NSSL. The finer grid + true-hail microphysics grew a more expansive storm: anvil filled the 120 km domain from t~65 min (benign -- open/radiative BCs, wbc..nbc=2, so it advects out, not wraparound), but REAL low-level convection (|w| up to 17 m/s and a 47 dBZ precip core at z=3-6 km, NOT damping-layer noise) reached the +y/-x open boundary in the final ~12 min (t>=108 min). The primary left/right mover updraft cores (+55/-50 m/s) stayed mid-level and interior throughout, and the split itself was textbook (left mover NW-drifting, right mover near-centre, Bunkers umove/vmove verified good) -- so the physics was sound and only the DOMAIN was marginal. Owner call (2026-07-21): re-run larger rather than accept-with-caveat or truncate. 540 gives ~15-20 km clearance to the fast-growing +y convection edge (which reached ~60 km clipped and was still advancing ~1.3 km/min). Cost ~2.25x the 360 run (~420 GB raw, within the 1024 GB VHDX; ~4.5 h wall-clock at np=8). The bubble stays domain-centred (iorigin=2), so the split reproduces identically with room to spare. Vertical grid unchanged (nz=40, dz=500, ztop 19.75 km).",

      "timax": 7200.0,
      "tapfrq": 12.0,
      "dtl": 2.0,
      "adapt_dt": 1,
      "rstfrq": 3600.0,

      "_timing_note": "2 h storm (timax=7200), matching the Phase 0 validation window in which the split fully develops (2 cores separated by 75 min, 46 km apart by 120 min). tapfrq=12 s -> 601 output frames, the same 12 s cadence as single_cell (owner's choice for dense playback); heavier raw I/O than 301 frames but well within the VHDX budget. dtl=2.0 s is the CFL-limited 333 m step (docs/phase0-benchmark.md 4); adapt_dt=1 adapts from there.",

      "_rstfrq_note": "OPTIONAL run-control override (deck.py Category 5), NOT part of the scenario identity. Hourly restart files (rstfrq=3600 s). The template default is -3600 (restarts OFF), verified: the first 360-run wrote no rst*.nc. This enlarged run is ~4.5 h -- past the charter's 'restarts safely skippable only on short 2-3 h runs' line -- so a mid-run crash costs time, not a full re-run. restart_reset_frqtim=.true. and restart_filetype=2 (netCDF) come from the template &param16.",

      "isnd": 5,
      "iwnd": 2,
      "iinit": 1,
      "irandp": 0,
      "icor": 0,
      "imove": 1,
      "iorigin": 2,

      "_storm_note": "isnd=5 WK analytic sounding; iwnd=2 the WK unidirectional shear profile that makes a supercell (the single defining change from the pulse cell); iinit=1 single warm bubble (irandp=0 -- so two diverging cores can ONLY be a split, not independent cells, exactly as Phase 0 argued); icor=0 (the canonical WK case runs Coriolis-off); imove=1 Bunkers moving frame; iorigin=2 centred coordinates. Output x/y are in the MOVING frame, so the domain stays centred at (0,0) and the symmetric-about-origin export box (the only mode the pipeline supports) is exactly right: the right mover sits near (0,0), the left mover's NW drift becomes the box's asymmetric bulge, and the box CENTRE stays static at (0,0) -- the SVT constraint, satisfied for free.",

      "umove": 12.5,
      "vmove": 3.0,

      "_motion_note": "Bunkers right-mover storm-motion estimate, inherited from the Phase 0 validation deck (docs/phase0-validation.md). REQUIRED (not derivable) whenever imove=1 -- deck.py refuses imove=1 without them. This scenario is the FIRST positive exercise of the imove=1 branch in the deck generator; it was previously reached only by negative controls (Phase 2 10.4).",

      "ptype": 27,
      "ihail": 1,

      "_micro_note": "NSSL 2-moment, ptype=27 (true hail category) -- the production microphysics, unchanged from single_cell. The Phase 0 supercell was validated with Morrison (ptype=5); this is the FIRST NSSL supercell, so the split's development is eyeballed in the output (plan 3, watch-item 3; formally checked in T2).",

      "terrain_flag": false,
      "itern": 0,
      "stretch_z": 0
    },

    "provenance": {
      "cm1_version": "cm1r21.1",
      "build_doc": "docs/phase0-cm1-build.md",
      "namelist": "GENERATED by pipeline/gen_deck.py from this file -- no hand-written deck exists for this scenario.",
      "microphysics": "NSSL 2-moment, ptype=27 (true hail category)",
      "sounding": "Weisman-Klemp analytic (isnd=5)",
      "shear": "WK unidirectional shear (iwnd=2) -- the supercell-producing profile, validated in Phase 0",
      "initiation": "single warm bubble (iinit=1), no random perturbations (irandp=0)",
      "domain_motion": "moving, Bunkers right-mover (imove=1, umove=12.5, vmove=3.0)"
    }
  },

  "export": {
    "_provisional": true,

    "_note": "PLACEHOLDER box -- NOT yet measured on the ENLARGED (540) run. The crop box is an OUTPUT of the run, measured from this run's own active-voxel union with `export_scenario.py bbox`, then written back here with _provisional dropped (T1 'measured box' deliverable; require_measured_box refuses export until then). The values below are INFORMED guesses from the first (360) run, so the config loads and the deck can be generated. Unlike every prior scenario this storm is ASYMMETRIC (splitting + NW-drifting left mover), so the measured half-width must actually REACH the left mover: a box that clips it silently defeats the T3/T9 cref orientation test that consumes this asset. NOTE the 360-run box came back as the FULL domain (anvil fills it); on the 540 run the anvil will again spread widely, so the measured horizontal box will likely be large -- that is fine (web-only this phase, no SVT static-bbox penalty). What matters is that the CONVECTION and both movers sit inside with clearance.",

    "voxel_m": 333.0,

    "_voxel_note": "NATIVE 333 m -- the sim grid, no resampling (same rule as single_cell_333m: the export voxel tracks the simulation resolution).",

    "crop_half_width_m": 85248.0,

    "_half_width_note": "PROVISIONAL. nx=512 placeholder (2 x 85248 / 333 = 512). Guessed from the 360-run measurement: real convection reached ~60 km (clipped, still growing) and the anvil filled the whole domain, so the true half-width on the 540 grid is expected to be large (anvil-driven). Will be replaced by the measured union half-width. active_mask does NOT include w (signed, web-only), so the exporter separately checks the w extent; on the 360 run w>=5 reached the model top, but that upper-level w sits inside the zd=15 km Rayleigh sponge (damped, not clean updraft) -- the honest z-top is the condensate anvil top, below.",

    "crop_z_top_m": 17649.0,

    "_z_top_note": "PROVISIONAL. nz=53 placeholder (17649 / 333 = 53). The 360 run measured a condensate anvil TOP of 17.25 km; w>=5 reached 19.25 km but that is inside the zd=15 km damping layer (numerical, not clean updraft), so the export z-top is set from the condensate top, not the damped w. The 540 run's measured condensate top replaces this."
  }
}
